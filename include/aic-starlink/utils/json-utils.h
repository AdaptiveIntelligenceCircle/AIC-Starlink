#pragma once 

#include <optional> 
#include <string> 

#include "error.h"
using namespace std; 

namespace aic_starlink :: utils 
{
    class JsonUtils 
    {
        public: 
        // static core :: Error loadFile(const string &path, string &out_text); 
        // static core :: Error writeFile(const string &path, const string &text);
        
        // [[nodiscard]] static bool isValidJson(const string &text); 
    }; 
} // namespace aic_starlink :: utils.
